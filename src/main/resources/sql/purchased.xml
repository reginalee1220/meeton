<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="purchased">

    <select id="p_getAka" parameterType="int" resultType="String">
        select aka from channel where channelnum=#{channelNum}
    </select>

    <update id="p_updatesub" parameterType="int">
        update channel set subscribers=subscribers+1 where channelnum=#{channelNum}
    </update>

</mapper>